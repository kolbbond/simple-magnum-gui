build/*
!build/README.md
.clangd
build
.cache
lib
dependencies
.deps
imgui.ini

# cross-build scratch (wasm: native corrade-rc + emscripten output)
build-native
build-wasm

# out-of-tree build dirs (build-ci, build-full, etc.)
build-*/

# stray top-level clones — the real ones live under external/ as submodules
/implot
/implot3d
