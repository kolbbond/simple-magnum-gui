build/*
!build/README.md
.clangd
build
.cache
lib
dependencies
.deps
imgui.ini

# cross-build scratch (wasm: native corrade-rc + emscripten output)
build-native
build-wasm

# stray top-level clones — the real ones live under external/ as submodules
/implot
/implot3d

# agent scratch — AI plans/specs, analyses, panopticon memory (kept local, never tracked)
docs/superpowers/
docs/analyses/
.pan/
